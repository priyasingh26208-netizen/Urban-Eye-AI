:80 {
    @api path /api/v1/*
    handle @api {
        reverse_proxy civiclens-backend:8000
    }

    @health path /health /health/*
    handle @health {
        reverse_proxy civiclens-backend:8000
    }

    handle {
        reverse_proxy civiclens-client:80
    }

    encode gzip
}